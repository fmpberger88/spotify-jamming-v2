{
  "name": "jammming-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/index.js",
    "build": "npm run build:frontend && npm run build:backend"
  },
  "dependencies": {
    "concurrently": "^8.2.1",
    "cross-env": "^7.0.3"
  }
}
